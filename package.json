{
  "name": "@indiekit/just",
  "version": "0.1.0",
  "description": "A command runner - TypeScript port of casey/just",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "just-run": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@types/node": "^25.3.0",
    "tsup": "^8.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
